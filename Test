//今天写关于正则表达式中遇到的一个问题
//且看以下代码

public class Demo1 {
    public  static void main(String[] args) {
        String str1 = "Hello World";
        String str2 = "HelloWorld";
     //如果此处直接调用会报错

    }

    //验证用户的名字是否满足需求，大小写字母，数字，下划线_ 4-16
    public boolean method1(String str){
        boolean b = str.matches("[a-zA-Z0-9_]{4,16}");
        return b;
    }
}


//为什么没办法直接调用方法，因为没有创建该类的对象
//两种解决方法
//1创建对象后调用
public static void main(String[] args) {
    Demo1 d = new Demo1();

    boolean result = d.method1("abc_12");

    System.out.println(result);
}
//2直接改为静态加上static
public static boolean method1(String str){}
//在主函数就可以直接调用
boolean result = method1("abc_12");

//---------------------------------------------------------------------------------------

//继正则表达式之后又学到了如何爬取网站前端信息，在这里将测试案例写出，之后如需使用，只需要更改正则规则和网址即可
//下面是代码演示,注释将不再赘述
URL url = new URL("网址");
URLConnection conn = url.openConnection();
BufferedReader br = new BufferedReader(new InputStreamReader(conn.getInputStream()));
String line;
String regex = "这里写正则表达式的规则"
Pattern pattern = Pattern.compile(regex);
while((line = br.readLine()) != null){
    Matcher matcher = pattern.matcher(line);
    whlie(matcher:find()){
       sout(matcher.group());
    }

}



